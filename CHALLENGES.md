
# 🚀 Наступний крок після наповнення бази даних: SQL‑челенджі (Interview‑Ready)

Після створення та наповнення бази **наступний логічний етап — навчитися витягувати з неї цінність**. Нижче наведений набір SQL‑челенджів у стилі продуктової Data Engineering співбесіди (Jooble / Product company).

---

### 🔹 SQL Challenge 1 — Health Check даних

**Завдання:** знайти всі вакансії з логічно некоректними датами.

Очікується:
- виявлення `last_updated < posted_at`
- підрахунок кількості проблемних записів

---

### 🔹 SQL Challenge 2 — Deduplication

**Завдання:** знайти дублікати у `job_views`.

Умови:
- однакові `(job_id, user_id, viewed_at)`
- показати кількість дублікатів

Бонус:
- показати, як залишити лише один запис (ROW_NUMBER).

---

### 🔹 SQL Challenge 3 — Popular Jobs

**Завдання:** знайти топ‑10 вакансій за кількістю переглядів за останні 30 днів.

Очікується:
- JOIN `jobs` + `job_views`
- фільтрація по даті
- агрегація + ORDER BY

---

### 🔹 SQL Challenge 4 — Funnel Analysis

**Завдання:** порахувати конверсію:

`views → applications → hired`

по кожній категорії вакансій.

Очікується:
- кілька CTE
- LEFT JOIN
- обробка NULL

---

### 🔹 SQL Challenge 5 — Time to Apply

**Завдання:** порахувати середній час від перегляду вакансії до подачі заявки.

Умови:
- використовувати точні дані (`job_views` + `job_applications`)
- результат у хвилинах

---

### 🔹 SQL Challenge 6 — SCD: останній статус

**Завдання:** для кожної вакансії визначити її **останній статус**.

Очікується:
- window function (`ROW_NUMBER` або `MAX(changed_at)`)
- GROUP BY або PARTITION BY

---

### 🔹 SQL Challenge 7 — Active Time

**Завдання:** порахувати, скільки часу вакансія була у статусі `active` (виключаючи `paused`).

Очікується:
- LEAD / LAG
- робота з інтервалами часу

---

### 🔹 SQL Challenge 8 — Late Arriving Data

**Завдання:** знайти записи в `job_status_history`, де порядок статусів порушений у часі.

Очікується:
- сортування
- порівняння з попереднім записом

---

### 🔹 SQL Challenge 9 — Performance

**Завдання:**
1. знайти повільний запит (JOIN + aggregation)
2. показати `EXPLAIN ANALYZE`
3. запропонувати індекс

---

### 🔹 SQL Challenge 10 — Business Question

**Завдання:**

> Які компанії отримують найбільше наймів, але мають низьку конверсію з переглядів?

Це питання перевіряє:
- бізнес‑мислення
- вміння комбінувати факти
- аналітичну інтуїцію

---

## 🎯 Як використовувати ці челенджі

Рекомендований формат:
1. написати SQL
2. перевірити коректність результату
3. оптимізувати
4. вміти **пояснити рішення вголос**

Якщо ти можеш спокійно пройти ці 10 задач — ти готовий до реальної технічної співбесіди.

---

## 🧠 Далі за планом

Наступні можливі кроки:
- індекси та performance‑тюнінг
- dbt‑моделі поверх цієї бази
- mock‑інтервʼю (питання + live SQL)

---

